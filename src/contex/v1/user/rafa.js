import { Router } from "express";
import { isClassElement } from "typescript";

const obj = {
    name: "Rafael",
    age: 22,
    isIntern: true,
    hobbies: ["fishing", "drinkin"],
    wekeend: [
        {job1: "study", time: 10},
        {job1: "party", time: 20}
    ]
}

// @Controller
// @Route('/api/v1/users', GET)
// function getUsers();


// module.exports = new Class()

// class = require('class')